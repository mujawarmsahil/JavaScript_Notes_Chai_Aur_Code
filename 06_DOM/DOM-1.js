//we can access the html document of any site by using the command windows.document or document 
//if we want to see the access the information which is under the document using (.) operator doucment.bseURL
//links in document are HTML collections they are not by default arrays but we convert the into arrays

//There are selectors to choose the elements from documenr
//Following are the some of them
//1:- getElementById("Idname");
// we can access the classes , attribute  of that particular id 